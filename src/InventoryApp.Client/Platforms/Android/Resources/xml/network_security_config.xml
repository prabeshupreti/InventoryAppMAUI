<?xml version="1.0" encoding="utf-8"?>
<!--
  Development convenience only: allows cleartext traffic to the emulator loopback host
  so the app can also be pointed at the plain-HTTP endpoint (port 5266) while testing.
  Remove this file, and the manifest reference to it, before shipping a release build.
-->
<network-security-config>
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">10.0.2.2</domain>
    <domain includeSubdomains="true">localhost</domain>
  </domain-config>
</network-security-config>
